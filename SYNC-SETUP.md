# ตั้งค่าซิงก์ข้อมูลลูกค้าผ่าน Google Sheets (ทำครั้งเดียว)

ระบบนี้ทำให้หลายเครื่อง/หลายคนเห็นรายการลูกค้าชุดเดียวกัน โดยใช้ Google Sheets เป็นฐานข้อมูล
และ Google Apps Script เป็น API เชื่อมระหว่างเว็บกับชีต ไม่มีค่าใช้จ่าย ใช้บัญชี Google ที่มีอยู่แล้วได้เลย

## ขั้นตอน

1. เปิด [Google Sheets](https://sheets.google.com) แล้วสร้างสเปรดชีตใหม่ 1 ไฟล์ (ตั้งชื่ออะไรก็ได้ เช่น "Badge Calculator Orders")
2. ในสเปรดชีต ไปที่เมนู **ส่วนขยาย (Extensions) → Apps Script**
3. ลบโค้ดเริ่มต้นในไฟล์ `Code.gs` ทั้งหมด แล้วคัดลอกเนื้อหาทั้งหมดจากไฟล์ `apps-script.gs` ในโปรเจกต์นี้ไปวางแทน
4. กด **บันทึก** (ไอคอนแผ่นดิสก์ หรือ Ctrl+S)
5. กด **Deploy → New deployment**
   - คลิกไอคอนรูปเฟือง (Select type) แล้วเลือก **Web app**
   - Description: ใส่อะไรก็ได้ เช่น "Orders API"
   - Execute as: **Me**
   - Who has access: **Anyone**
   - กด **Deploy**
6. ระบบจะขอสิทธิ์ (authorize) — กด **Authorize access** เลือกบัญชี Google ของคุณ แล้วกด **Advanced → Go to (ชื่อโปรเจกต์) (unsafe) → Allow** (ข้อความเตือน "unsafe" เป็นเรื่องปกติสำหรับสคริปต์ที่เขียนเอง ไม่ได้ผ่านการรีวิวจาก Google)
7. เมื่อ deploy สำเร็จ จะได้ **Web app URL** ลักษณะ `https://script.google.com/macros/s/xxxxxxxx/exec` — คัดลอก URL นี้ไว้
8. เปิดหน้า `customers.html` ในเว็บ badge-calculator แล้ววาง URL นี้ในช่อง "วาง URL ของ Google Apps Script Web App" แล้วกด **บันทึก URL**

เท่านี้ทุกเครื่องที่ตั้งค่า URL เดียวกันจะเห็นรายการลูกค้าชุดเดียวกัน (ข้อมูลถูกเก็บในชีตชื่อ "Orders"
ที่ระบบสร้างให้อัตโนมัติ ส่วนรูปใบสั่งผลิตจะถูกอัปโหลดไปเก็บในโฟลเดอร์ Google Drive ชื่อ
"BadgeCalcOrderImages" โดยอัตโนมัติเช่นกัน)

## ถ้าต้องแก้โค้ด Apps Script ภายหลัง

หลังแก้โค้ดใน Apps Script editor แล้ว ต้องทำ **Deploy → Manage deployments → แก้ไข (ไอคอนดินสอ)
→ Version: New version → Deploy** ทุกครั้ง ไม่งั้นการแก้ไขจะไม่มีผลกับ URL เดิม

## ข้อจำกัดที่ควรรู้

- ต้องมีอินเทอร์เน็ตตอนบันทึก/ซิงก์ ถ้าออฟไลน์ระบบจะยังบันทึกเก็บไว้ในเครื่องนั้นๆ ได้ตามปกติ
  แต่จะไม่ขึ้นในเครื่องอื่นจนกว่าจะซิงก์สำเร็จ
- Apps Script มีโควตาการใช้งานฟรีต่อวัน (เช่น จำนวนครั้งที่เรียก URL Fetch/Drive) ซึ่งเพียงพอสำหรับ
  ร้านค้าขนาดเล็ก-กลาง แต่ถ้ามีออเดอร์จำนวนมากมากๆ ต่อวันอาจต้องพิจารณาย้ายไปใช้ฐานข้อมูลอื่น
- ทุกคนที่มี URL นี้สามารถอ่าน/แก้/ลบข้อมูลได้ทั้งหมด (ไม่มีระบบล็อกอิน/สิทธิ์ผู้ใช้แยก) จึงไม่ควรแชร์ URL
  นี้กับคนนอกทีม
