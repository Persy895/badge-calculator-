# ตั้งค่าซิงก์ข้อมูลลูกค้าผ่าน Firebase (ทำครั้งเดียว)

ระบบนี้ทำให้หลายเครื่อง/หลายคนเห็นรายการลูกค้าชุดเดียวกันแบบเรียลไทม์ (ไม่ต้องกดซิงก์เอง อัปเดตสด
ทันทีที่มีใครแก้ไข) โดยใช้ Firebase Firestore เป็นฐานข้อมูล ไม่มีค่าใช้จ่าย (อยู่ในโควตาฟรี Spark plan)

## ขั้นตอน

1. เปิด [Firebase Console](https://console.firebase.google.com) แล้วกด **Add project** สร้างโปรเจกต์ใหม่
   (ปิด Google Analytics ได้ ไม่จำเป็นสำหรับงานนี้)
2. ในเมนูซ้าย ไปที่ **Build → Firestore Database** กด **Create database**
   - เลือก location ที่ใกล้ (เช่น asia-southeast1)
   - เลือกโหมด **Start in test mode** (จะเปิดให้อ่าน/เขียนได้ 30 วัน — ดูขั้นตอนที่ 5 เพื่อตั้ง rule แบบถาวร)
3. ไปที่ **Project settings** (ไอคอนเฟืองมุมบนซ้าย) → แท็บ **General** → เลื่อนลงมาที่ **Your apps**
   กด ไอคอน **</>** (Web) เพื่อสร้างเว็บแอป ตั้งชื่ออะไรก็ได้ แล้วกด **Register app**
4. จะเห็นโค้ดตัวอย่างมี `const firebaseConfig = { apiKey: "...", authDomain: "...", ... }` —
   คัดลอกเฉพาะส่วน `{ ... }` (ก้อนวงเล็บปีกกา) ทั้งหมด
5. ไปที่ **Firestore Database → Rules** แล้ววางกฎนี้แทนของเดิม จากนั้นกด **Publish**:
   ```
   rules_version = '2';
   service cloud.firestore {
     match /databases/{database}/documents {
       match /{document=**} {
         allow read, write: if true;
       }
     }
   }
   ```
   (กฎนี้เปิดให้ใครก็ตามที่มีค่า config นี้อ่าน/เขียนข้อมูลได้ทั้งหมด เหมาะกับการใช้งานภายในทีมเล็กๆ
   ที่ไว้ใจกัน — อย่าเผยแพร่ค่า config นี้ให้คนนอกทีม)
6. เปิดหน้า `customers.html` วางก้อน config ที่คัดลอกมาในขั้นตอนที่ 4 ลงในช่อง
   "วางค่า Firebase config..." แล้วกด **เชื่อมต่อ Firebase**

เท่านี้ทุกเครื่องที่วาง config ชุดเดียวกัน (บน `index.html` และ `customers.html`) จะเห็นรายการลูกค้า
และเห็นการเปลี่ยนสถานะของกันและกันแบบเรียลไทม์ทันที ไม่ต้องกดซิงก์เอง

## ข้อจำกัดที่ควรรู้

- **รูปใบสั่งผลิตไม่ซิงก์ข้ามเครื่อง** — เครื่องที่กด "บันทึกภาพ" จะเห็น/ดาวน์โหลดรูปได้ตามปกติ
  แต่เครื่องอื่นจะไม่เห็นรูปนั้น (เห็นแค่ข้อมูลชื่อ/รายการ/ราคา/สถานะ) เพราะ Firestore จำกัดขนาดต่อ
  รายการไว้ 1MB ซึ่งรูปใบสั่งผลิตอาจเกินได้ง่าย การจะซิงก์รูปด้วยต้องใช้ Firebase Storage เพิ่ม
  ซึ่งปัจจุบัน Google กำหนดให้ต้องอัปเกรดเป็นแผนเสียเงิน (Blaze) จึงยังไม่รวมไว้ในเวอร์ชันนี้
- ต้องมีอินเทอร์เน็ตตอนบันทึก/ซิงก์ ถ้าออฟไลน์ระบบจะยังบันทึกเก็บไว้ในเครื่องนั้นๆ ได้ตามปกติ
  แต่จะไม่ขึ้นในเครื่องอื่นจนกว่าจะกลับมาออนไลน์
- กฎ Firestore ด้านบนเปิดกว้างให้ใครก็ตามที่มี config นี้แก้ไขข้อมูลได้ทั้งหมด (ไม่มีระบบล็อกอิน)
  จึงไม่ควรแชร์ค่า config ให้คนนอกทีม
- Firestore free tier (Spark plan) ให้โควตาฟรีต่อวันค่อนข้างสูง (ปกติเพียงพอสำหรับร้านค้าขนาดเล็ก-กลาง)
  แต่ถ้ามีการใช้งานหนักมากอาจต้องพิจารณาอัปเกรดแผน
